name: Inputs Demo
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Choose environment to Deploy
        required: true
        default: dev
        type: choice
        options:
          - dev
          - test
          - prod
      artifact_tag:
        description: Choose git tag to deploy
        required: true
jobs:
  inputs-demo: 
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the app
        run: | 
         echo "Deploy the application to ${{inputs.environment}}"
         echo "Deploying the artifact ${{inputs.artifact_tag}} of the application to ${{ inputs.environment}}"
          

        
